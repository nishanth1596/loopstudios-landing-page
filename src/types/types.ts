export type LogoProps = {
  img: string;
  ariaLabel: string;
  onClick: () => void;
};

export type OurCreationProps = {
  image: string;
  altText: string;
  description: string;
};

export type NavBarProps = {
  footer?: boolean;
};
